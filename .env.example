# Copier ce fichier en ".env" et renseigner votre cle API Anthropic.
# Sans cle (ou avec un compte sans credit), l'API bascule automatiquement
# en mode extractif (reponses sans generation IA, cf. README).
ANTHROPIC_API_KEY=
